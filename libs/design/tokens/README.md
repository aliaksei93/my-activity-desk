# Design Tokens

Framework-agnostic source of truth for visual primitives:

- color tokens
- spacing scale
- radii
- shadows
- global theme CSS variables

Angular, React, Vue, and Storybook hosts should consume this layer instead of
defining framework-specific token copies.
